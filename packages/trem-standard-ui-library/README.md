# Trem Standard UI Library

Storybook workspace for viewing shared Trem UI components in one place.

## Commands

```sh
pnpm storybook
pnpm build:storybook
```

Component stories live in `src/stories` and import the shared package from `@packages/trem-ui`.
