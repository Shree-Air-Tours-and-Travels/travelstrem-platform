# Future Amadeus adapter

Implement `FlightProvider` in this folder and register it in `../flight-provider.factory.js`. Authentication, Amadeus request/response objects, status translation, and error mapping must remain inside the adapter. Export only normalized TravelsTREM flight models.

Required server configuration is documented in `docs/flights-provider-integration.md`.
